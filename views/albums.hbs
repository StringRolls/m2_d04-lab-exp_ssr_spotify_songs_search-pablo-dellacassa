<div class="album-list container">
{{#each data}}
<div class="album-card">

<h1>{{name}}</h1>
<img src="{{images.[0].url}}" alt="{{name}}" style="width: 160px">
<a href="/tracks/{{this.id}}">View tracks</a>
</div>
{{/each}}
</div>
